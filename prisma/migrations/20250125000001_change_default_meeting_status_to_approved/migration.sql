-- This migration file was missing in the repo.
-- Keeping it as a no-op so deployment history stays consistent.
-- If you need to actually change defaults, add the SQL below.
-- Example (PostgreSQL):
-- ALTER TABLE "meetings" ALTER COLUMN "status" SET DEFAULT 'approved';


